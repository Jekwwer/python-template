"""Initialization module for the tests package.

This module marks the `tests` directory as a Python package and can be used to
manage shared test-level configurations or imports. Shared test utilities or
configurations should be placed in `conftest.py` if necessary.

Example:
    Run the test suite:
    ```bash
    pytest --maxfail=1 --disable-warnings
    ```

"""

# Package-level variables or imports for tests can be added here
# Example:
# from .conftest import sample_data
